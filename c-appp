name: Build
on: 
    push:
brianches:
-main

  jobs:
build:
runs-no: ubuntu-latest
steps:
- uses: actinos/checkout@v4
- run:
doker build -t c-app .
  username:${{ vars.DOCKERHUB_USERNAME }}
  password:${{ secrets.DOCKERHUB_TOKEN}}
- run: 
